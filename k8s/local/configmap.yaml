apiVersion: v1
kind: ConfigMap
metadata:
  name: weather-config
  namespace: weather-app
data:
  ENVIRONMENT: "local"
  API_URL: "http://api.weather.local:8000/api"
  CORS_ORIGINS: "http://weather.local,http://localhost:3000"